# python-network_1

This project covers making HTTP requests in Python using both the
urllib and requests libraries — fetching data, reading headers,
sending POST parameters, and handling error codes.
